node_modules/
.next/
.vercel/
out/
*.tsbuildinfo
.env*.local
# api/_vendor/tradecraft is checked in (regenerated by scripts/vendor-tradecraft.sh
# on each release) so Vercel can deploy with web/ as the project root without
# needing the "Include source files outside of Root Directory" toggle.
